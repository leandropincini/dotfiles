# installing a global package with pip
syspip() {
    PIP_REQUIRE_VIRTUALENV="" pip "$@"
}

docker-join() {
    docker exec -it $1 bash
}

dlaudio() {
    youtube-dl -f bestaudio --extract-audio --audio-format mp3 $1"
}
